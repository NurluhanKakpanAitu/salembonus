# SalemBonus frontend

Vite + React + TypeScript PWA. Толық нұсқаулық жоба түбіріндегі README.md файлында.

```bash
npm install
npm run dev      # http://localhost:5173, /api -> http://localhost:5113
npm run build    # dist/ + service worker
```
